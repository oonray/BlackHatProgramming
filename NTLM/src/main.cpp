#include <curl/curl.h>
#include <iostream>
#include <pair>
#include <string>
#include <thread>
#include <utility>
#include <vector>

#include <curl/curl.h>

std::vector<std::thread *> *threads;
std::vector<std::pair<std::string, std::string> *> creds;

void Check(std::string user, std::string pass)

    int main(int argc, char *argv[]) {
  threads = new std::vector<std::thread *>;
  return 0;
}
